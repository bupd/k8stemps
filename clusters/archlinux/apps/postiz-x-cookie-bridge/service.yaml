apiVersion: v1
kind: Service
metadata:
  name: postiz-x-cookie-bridge
  namespace: postiz
  labels:
    app.kubernetes.io/name: postiz-x-cookie-bridge
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postiz-x-cookie-bridge
  ports:
    - name: http
      port: 80
      targetPort: http
