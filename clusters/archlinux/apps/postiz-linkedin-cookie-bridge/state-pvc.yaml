apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postiz-linkedin-cookie-bridge-state
  namespace: postiz
  labels:
    app.kubernetes.io/name: postiz-linkedin-cookie-bridge
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi
